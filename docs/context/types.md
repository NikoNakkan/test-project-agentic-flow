# Shared types

Top node = type file path. One row per exported type.

| symbol_id | name | kind | used_by |
|-----------|------|------|---------|
| packages/contract/types.ts | HelloWorldCount | interface | fe-dev (HelloWorldButton, useHelloWorldCount) |

<!-- kind: interface | type | class | enum | pydantic_model -->
